#include<stdio.h>
#include<string.h>
void main()
{
    int a,b,i,s;
    printf("Enter the two numbers:");
    scanf("%d%d",&a,&b);
    for(i=0;i<=x||i<=y;i++)
    {
        if(i%a==0&&i%b==0)
        {
            s=i;
        }
    }
    printf("\nThe smallest number divide by both a&b are:%d",s);
    }
